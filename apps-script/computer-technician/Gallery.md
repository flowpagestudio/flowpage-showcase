# Gallery management

Five public slots: `gallery-1` … `gallery-5`.

- Seeded from `flowpage-assets` on `setupProject()`
- Owner replaces images from `?view=management`
- Public site reads `?view=gallery&callback=FlowPageGallery`
- Drive files are shared as “anyone with the link” so thumbnails work on GitHub Pages
